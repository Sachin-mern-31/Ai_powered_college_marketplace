{
  "name": "campusexchange-monorepo",
  "version": "1.0.0",
  "description": "CampusExchange - AI-Powered Student Marketplace Monorepo Package",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server/server.js",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "render-build": "npm run install:all && npm run build"
  },
  "author": "Sachin",
  "license": "MIT"
}
